name: qcpAPI_newver
channels:
  - defaults
dependencies:
  - python=3.11
  - typing_extensions # pydantic needs that but for some reason does not install by itsself with my installation
  - exceptiongroup
  - numpy
  - tqdm
  - psutil
  - build
  - pip
  - pip:
    - setuptools-scm
    - fastapi[standard]
    - sqlmodel
    - requests
    - psutil
    - rdkit
    - setuptools
    - git+ssh://git@github.com/bruno-von-bruening/qcp_global_utils.git@4e53544bd
    - git+ssh://git@github.com/bruno-von-bruening/qcp_objects.git@d32cfc3db
    - git+ssh://git@github.com/bruno-von-bruening/qcp_orm.git@1e2a8d58f
    - ..
