name: qcpAPI_newver
channels:
  - defaults
dependencies:
  - python=3.11
  - typing_extensions # pydantic needs that but for some reason does not install by itsself with my installation
  - exceptiongroup
  - numpy
  - tqdm
  - pip
  - pip:
    - setuptools-scm
    - fastapi[standard]
    - sqlmodel
    - requests
    - psutil
    - rdkit
    - setuptools
    - git+ssh://git@github.com/bruno-von-bruening/qcp_global_utils.git@812919b6c
    - git+ssh://git@github.com/bruno-von-bruening/qcp_objects.git@b1309d414
    - git+ssh://git@github.com/bruno-von-bruening/qcp_orm.git@35b4f03df
  - build
