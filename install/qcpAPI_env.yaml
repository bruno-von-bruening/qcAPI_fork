name: qcpAPI_newver
channels:
  - defaults
dependencies:
  - python=3.11
  - typing_extensions # pydantic needs that but for some reason does not install by itsself with my installation
  - exceptiongroup
  - numpy
  - tqdm
  - pip
  - pip:
    - setuptools-scm
    - fastapi[standard]
    - sqlmodel
    - requests
    - rdkit
    - setuptools
    - git+ssh://git@github.com/bruno-von-bruening/qcp_global_utils.git@10df6fc64
    - git+ssh://git@github.com/bruno-von-bruening/qcp_objects.git@b1309d414
    - git+ssh://git@github.com/bruno-von-bruening/qcp_orm.git@afa782bea
  - build
