name: qcAPI
channels:
  #- conda-forge
  #- conda-forge/label/libint_dev
  - defaults
dependencies:
  - python=3.10
  #- git
  - numpy
  #- psi4
  - tqdm
  - pip
  - pip:
    - fastapi[standard]
    - sqlmodel
    - requests
    - rdkit
    - setuptools
    - build
